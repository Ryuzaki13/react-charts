/**
 * Анимирует только визуальное состояние точки. Координаты `cx` и `cy` намеренно
 * исключены, чтобы при первом измерении графика точка сразу занимала своё место.
 */
export const datumElementTransition =
    "r .3s ease-out, opacity .3s ease-out, fill .3s ease-out, stroke .3s ease-out, stroke-width .3s ease-out";
